<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.platform.mapper.CartMapper">

    <resultMap id="CartResultMap" type="com.project.platform.entity.Cart">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="session_id" property="sessionId"/>
        <result column="item_count" property="itemCount"/>
        <result column="total_quantity" property="totalQuantity"/>
        <result column="total_amount" property="totalAmount"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 根据用户ID查询购物车 -->
    <select id="selectByUserId" resultMap="CartResultMap">
        SELECT * FROM carts WHERE user_id = #{userId}
    </select>

    <!-- 根据会话ID查询购物车 -->
    <select id="selectBySessionId" resultMap="CartResultMap">
        SELECT * FROM carts WHERE session_id = #{sessionId}
    </select>

</mapper>